# Use a long random value in production; do not commit the real .env file.
AGRI_SECRET_KEY=replace-with-a-long-random-secret
# Leave unset locally. Render uses /var/data to persist private workbooks and listings.
# APP_DATA_DIR=/var/data
